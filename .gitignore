# ── Secrets / config (NEVER commit) ──────────────────────────────
wrench_config.json
.env
.env.*
**/.env
**/.env.*
wrench_deploy/.env*
*.flag

# ── Databases & backups (large / regenerable) ────────────────────
*.db
*.db-shm
*.db-wal
*.db.*
*.bak
*.bak*
db_backups/

# ── Build outputs served from wrench_deploy/ are intentionally tracked
#    (Vercel deploys from git), but the stale root demo copy is not needed.
wrench_deploy/wrench_demo.html

# ── Quarantined unsafe one-shot import scripts (see folder README) ─
_archive_unsafe_imports/

# ── Logs ─────────────────────────────────────────────────────────
*.log

# ── Tooling / build artifacts ────────────────────────────────────
wrench-repomix.xml
.vercel
.vercel/
__pycache__/
*.pyc
*.pyo

# ── Installers / binaries (not source) ───────────────────────────
*.exe
*.msi
*.msix
*.zip
*.7z
*.dmg

# ── API response caches (large, regenerable) ─────────────────────
*_cache.json
complaints_api_cache.json

# ── Node deps (never commit) ─────────────────────────────────────
node_modules/
**/node_modules/

# ── Claude Code local state (machine-specific) ───────────────────
.claude/settings.local.json
.claude/*.lock

# ── CLAUDE.md project context ────────────────────────────────────
#    Root CLAUDE.md is COMMITTED (shared, sanitized methodology). Scoped +
#    local files stay untracked (internal process / sensitive ops notes).
wrench_deploy/CLAUDE.md
files/CLAUDE.md
CLAUDE.local.md
.claude.local.md

# ── OS cruft ─────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# Cloned OSS datasets (third-party, regenerable)
oss_import/

# transient caches / test scripts
nhtsa_safety_cache.json
_drainbolt_verify.py
_vf_raw.json

# Subaru OMs exceed GitHub 100MB limit - kept locally, not committed
_subaru_outback_*_om.pdf

# ── OEM manual PDFs + business/DoD docs (source material / private — NEVER commit) ──
#    All PDFs/docx are verification source material or private docs; none are shipped.
#    (2026-07-02 audit F9/F13/F14: 449MB of copyrighted GM manuals were tracked here.)
*.pdf
*.docx
#    OM plain-text extractions: the _OM* glob missed the real filenames
#    (_CADESC2021_full.txt, _TS2021_full.txt, _duramax30_full.txt, _silverado_2024_full.txt, ...)
#    plus the _*_caps.txt capacity dumps — all copyrighted OM text (2026-07-11 audit review).
_OM*_full.txt
_*_full.txt
_*_caps.txt
_pilot_manuals/
_mazda_manuals/

# ── Private business / strategy / DoD docs (untracked — NEVER commit). Listed
#    INDIVIDUALLY, not `KYR_*`: the KYR_*_Verification_Log.md + KYR_OEM_Manual_Source_Map.md
#    provenance docs are intentionally TRACKED and must stay committable.
KYR_Investor_Pitch.md
KYR_Military_Scalability_Assessment.md
KYR_DOT_SBIR_FY26_Fit_Analysis.md
KYR_LWI_Meeting_Notes.md
KYR_LWI_Meeting_Brief.md
KYR_Standalone_FSM_Sourcing.md
KYR_Verification_Sourcing_Cost_Analysis.md
KYR_Data_Rights_Marking_Guide.md
KYR_Mazda3_Curated_Specs_Needed*.csv
KYR_Researcher_Batch2_Honda_Acura.csv

# ── Misplaced clone + independently-deployed demo projects (0 tracked files each) ──
UsersRobertreposskills/
kyr-military-demo/
kyr-af-demo/

# ── files/ pipeline: bulk doc/binary junk only — the build scripts stay tracked ──
files/Army Docs/
files/Air Force Docs/
files/*.pdf

# ── Canonical DB: explicit (also covered by *.db) — intentionally local-only ──
wrench_vehicles.db
